/// @description So sprite doesn't constantly switch between frames and appears more transparent

// Making it so it doesn't switch between frames
image_speed = 0;

// setting image alpha lower so it appears translucent
image_alpha = 0.2;
