/// @description setting up variables + so object doesn't constantly switch between frames

// setting image speed to zero so object doesn't constantly switch between frames
image_speed = 0;

// variable which decides wether bounce pad has been pressed (happens for a few seconds after player touches it) or not
pressed = 0;
