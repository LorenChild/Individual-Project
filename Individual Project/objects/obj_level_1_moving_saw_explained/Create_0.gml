/// @description Setting up variables for timer and checking if player has touched object

// Variable for timing how long text stays appeared
timer = 500;

// variable for checking if player has touched the object for the first time
playerContact = 0;